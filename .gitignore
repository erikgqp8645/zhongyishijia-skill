# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Build artifacts
build/
dist/*.egg-info/

# Local overrides
.env.local
.env.*.local

# Reasonix local config
.reasonix/
reasonix.toml

# Temp query output
*.tsv

# Raw SQLite data (中医世家原始数据库，660MB，不进 git)
references/raw/*.sqlite
references/raw/*.db
references/raw/*.sqlite3

# LFS-tracked data (远端走 LFS, 本地 LFS server 失效时是真实数据副本, 不应再入 git)
# 已通过 release 下载: 不重复追踪
references/books_json/
references/text_distillation/evidence_cards.jsonl
references/raw/20120413mssql.sqlite

# 根目录误放的 books_json 副本(已删,但忽略未来重出)
/books_json/
